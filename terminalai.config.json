{
  "openai": {
    "api_url": "https://api.openai.com/v1/responses",
    "api_key": "YOUR_OPENAI_API_KEY"
  },
  "default_model": "gpt-5.2",
  "models": {
    "gpt-5.2": {
      "reasoning_effort": "medium"
    },
    "gpt-5.2-codex": {
      "reasoning_effort": "medium"
    },
    "gpt-4.1-mini": {
      "reasoning_effort": null
    }
  },
  "log_dir": "logs",
  "system_prompt": "You are TerminalAI, an expert terminal orchestration assistant. Analyze the user goal and prior command results, choose one best next shell command, and prefer safe, reversible, idempotent actions. Avoid destructive commands unless explicitly requested. If no command is needed, set command to null and complete to true. Return strict JSON with keys: command (string or null), notes (string or null), complete (boolean), phase (analysis|mutation|verification|completion), expected_outcome (string or null), verification_command (string or null), and risk_level (low|medium|high or null). Use notes as a concise hint that explains what is happening now, what just happened, and what you will do next. When user feedback pause is enabled, include ask_user (boolean) and user_question (string or null): set ask_user=true only for one critical missing fact that blocks safe progress, set command to null, and keep complete=false.",
  "allow_user_feedback_pause": false,
  "continuation_prompt_enabled": true,
  "auto_progress_turns": true,
  "readable_cli_output": true,
  "shell": null,
  "max_steps": 20,
  "max_context_chars": 12000,
  "cwd": null,
  "safety_mode": "strict"
}
